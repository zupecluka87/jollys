async function loadContent(){try{const res=await fetch('/data/site.json',{cache:'no-store'});const data=await res.json();heroMedia.style.backgroundImage=`url('${data.hero.image}')`;heroEyebrow.textContent=data.hero.eyebrow||'';heroTitle.textContent=data.hero.title||'';heroText.textContent=data.hero.text||'';introEyebrow.textContent=data.intro.eyebrow||'';introTitle.textContent=data.intro.title||'';introText.textContent=data.intro.text||'';projectsGrid.innerHTML=(data.projects||[]).map(p=>`<article class="project-card"><img src="${p.image}" alt="${p.title}"><div><h3>${p.title}</h3><p>${p.text}</p></div></article>`).join('');servicesGrid.innerHTML=(data.services||[]).map(s=>`<article class="service-card"><h3>${s.title}</h3><p>${s.text}</p></article>`).join('');contactTitle.textContent=data.contact.title||'';contactText.textContent=data.contact.text||'';phoneLink.textContent=data.contact.phone||'';phoneLink.href=`tel:${(data.contact.phone||'').replaceAll(' ','')}`;emailLink.textContent=data.contact.email||'';emailLink.href=`mailto:${data.contact.email||''}`;addressText.textContent=data.contact.address||''}catch(e){console.error(e)}}document.querySelector('.menu-btn').addEventListener('click',()=>document.querySelector('.nav').classList.toggle('open'));year.textContent=new Date().getFullYear();loadContent();